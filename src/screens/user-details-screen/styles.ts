import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {padding: 20, justifyContent: 'space-between', flex: 1},
  imageContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 10,
  },
  saveEditButtonsWrapper: {marginBottom: 10}
});
